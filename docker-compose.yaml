
name: flutter-desktop-panel

services:
  app:
    image: flutter-desk-panel-backend
    restart: unless-stopped
    environment:
      - QWEATHER_LOCATION=${LOCATION}
      - QWEATHER_KID=${KID}
      - QWEATHER_PROJECT_ID=${PROJECT_ID}
    volumes:
      - ./secrets:/app/app/secrets
      - ./cache:/app/app/cache
    ports:
      - "5000:5000"

